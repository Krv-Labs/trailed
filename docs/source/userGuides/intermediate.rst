.. _intermediate:

============
Intermediate
============

Tune direction sampling and descriptors for repeatability:

- increase ``num_thetas`` for richer signatures,
- set ``resolution`` to control curve granularity,
- use optional scale and normalization options.
