.. _installation:

============
Installation
============

.. code-block:: bash

   uv pip install -e .

Optional extras:

.. code-block:: bash

   uv pip install -e .[sklearn]
   uv pip install -e .[torch]
   uv pip install -e .[dataframe]
